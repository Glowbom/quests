import 'package:flutter/material.dart';

void main() {
  runApp(Quests());
}

class Quests extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Text('Glowbom Quests'),);
  }
}